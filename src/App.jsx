import { useEffect, useState } from 'react';
import { features } from './data/features';
import { templates } from './data/templates';

const BASE = '/animefusion';
const pathFor = (p) => `${BASE}${p === '/' ? '' : p}`;
const getRoute = () => { const p = window.location.pathname.replace(/\/$/, '') || '/'; return p === BASE ? '/' : p.startsWith(`${BASE}/`) ? p.slice(BASE.length) || '/' : '/'; };

function go(p) { window.history.pushState({}, '', pathFor(p)); window.dispatchEvent(new PopStateEvent('popstate')); window.scrollTo({ top: 0, behavior: 'instant' }); }
function Logo() { return <span className="logo"><span>AF</span></span>; }
function Link({ to, children, className='' }) { return <a className={className} href={pathFor(to)} onClick={(e) => { e.preventDefault(); go(to); }}>{children}</a>; }
function Button({ to, children, secondary=false }) { return <Link to={to} className={`btn ${secondary ? 'btn-secondary' : ''}`}>{children}<span>↗</span></Link>; }

function Nav() {
  const [open, setOpen] = useState(false);
  return <header className="nav-wrap"><div className="nav">
    <Link to="/" className="brand"><Logo/> <span>AnimeFusion</span></Link>
    <nav className={open ? 'nav-links mobile-open' : 'nav-links'}>
      <Link to="/">Home</Link><Link to="/templates">Templates</Link><Link to="/features">Features</Link>
    </nav>
    <div className="nav-actions"><Link to="/signin" className="signin">Sign in</Link><Button to="/signup">Get started</Button></div>
    <button className="menu" aria-label="Open navigation" onClick={() => setOpen(!open)}><i/><i/></button>
  </div></header>;
}

function ProductPreview() {
  return <div className="product-frame"><div className="window-bar"><span/><span/><span/><div>app.animefusion / overview</div><b>•••</b></div><div className="product-ui">
    <aside><div className="mini-brand"><Logo/> <b>AnimeFusion</b></div><small>WORKSPACE</small><em>Overview</em><span>Content</span><span>Providers</span><span>Templates</span><small>MANAGE</small><span>Users</span><span>Settings</span><div className="aside-foot">● Local instance</div></aside>
    <section><div className="ui-head"><div><small>OVERVIEW</small><h3>Your platform, your way.</h3></div><div className="status"><i/> Configured locally</div></div><div className="ui-grid"><div className="ui-panel large"><small>CORE ARCHITECTURE</small><div className="core-lines"><b>CONTENT</b><i>→</i><b>ROUTER</b><i>→</i><b>PROVIDER</b></div><p>Separate the system from the presentation layer.</p></div><div className="ui-panel"><small>ACTIVE LAYER</small><strong>Template</strong><span>Midnight</span></div><div className="ui-panel"><small>CONFIGURATION</small><strong>Self-hosted</strong><span>Infrastructure you control</span></div></div><div className="activity"><small>RECENT ACTIVITY</small><div><span>Provider configuration</span><b>Ready</b></div><div><span>Template layer</span><b>Connected</b></div><div><span>Core configuration</span><b>Local</b></div></div></section>
  </div></div>;
}

function WorkflowGraph() {
  const [active, setActive] = useState(1); const [locked, setLocked] = useState(false);
  useEffect(() => { if (locked) return; const id = setInterval(() => setActive(a => (a + 1) % 5), 1500); return () => clearInterval(id); }, [locked]);
  const nodes = [
    ['request','01','CONTENT REQUEST','Request received'], ['router','02','SMART ROUTER','Evaluating configured routes'], ['source','03','SOURCE SELECTED','Preferred source available'], ['ready','04','CONTENT READY','Route completed'], ['backup','05','BACKUP ROUTE','Fallback path available']
  ];
  return <div className="workflow-shell"><div className="workflow-top"><div><span className="live-dot"/> LIVE DEMO</div><button onClick={() => setLocked(!locked)}>{locked ? 'Unlock flow' : 'Pause on node'}</button></div><div className="workflow">
    <svg viewBox="0 0 1000 470" aria-hidden="true"><path className="wire" d="M170 235H335M500 235H665M500 235V365H665"/><path className="wire accent-wire" d="M170 235H335M500 235H665"/><circle className="packet p1" r="4"/><circle className="packet p2" r="4"/></svg>
    <div className={`wf-node n-request ${active===0?'active':''}`} onMouseEnter={() => setActive(0)} onClick={() => {setActive(0);setLocked(true)}}><small>01</small><b>CONTENT REQUEST</b><span>Request received</span></div>
    <div className={`wf-node n-router ${active===1?'active':''}`} onMouseEnter={() => setActive(1)} onClick={() => {setActive(1);setLocked(true)}}><small>02</small><b>SMART ROUTER</b><span>Evaluating configured routes</span><i>ROUTING</i></div>
    <div className={`wf-node n-source ${active===2?'active':''}`} onMouseEnter={() => setActive(2)} onClick={() => {setActive(2);setLocked(true)}}><small>03</small><b>SOURCE A</b><span>Preferred source</span><i>AVAILABLE</i></div>
    <div className={`wf-node n-ready ${active===3?'active':''}`} onMouseEnter={() => setActive(3)} onClick={() => {setActive(3);setLocked(true)}}><small>04</small><b>CONTENT READY</b><span>Route completed</span><i>READY</i></div>
    <div className={`wf-node n-backup ${active===4?'active':''}`} onMouseEnter={() => setActive(4)} onClick={() => {setActive(4);setLocked(true)}}><small>05</small><b>SOURCE B</b><span>Fallback route</span><i>BACKUP</i></div>
  </div><div className="workflow-info"><span>{nodes[active][1]} / {nodes[active][2]}</span><p>{nodes[active][3]}</p></div></div>;
}

function Architecture() { return <div className="architecture-diagram"><div className="arch-core"><small>ANIMEFUSION</small><strong>CORE</strong><span>Shared application logic</span></div><div className="arch-branches"><div>Content</div><div>Providers</div><div>Users</div><div>Configuration</div><div>Extensions</div></div><div className="arch-template"><small>PRESENTATION</small><strong>TEMPLATE</strong><span>Swap the visual layer</span></div></div>; }

function TemplatePreview({ t }) { return <div className={`template-preview ${t.tone}`}><div className="preview-top"><b>AF</b><span>Home&nbsp;&nbsp; Browse&nbsp;&nbsp; Search</span></div><div className="preview-main"><small>{t.label}</small><h3>{t.name}<em>.</em></h3><span>Explore library →</span></div><div className="preview-shapes"><i/><b/><span/></div><div className="preview-rows"><i/><i/><i/><i/></div></div>; }
function TemplateCard({ t }) { return <article className="template-card"><Link to={`/templates/${t.slug}`} className="template-link"><TemplatePreview t={t}/><div className="template-arrow">↗</div></Link><div className="template-meta"><div><small>{t.label}</small><h3>{t.name}</h3></div><span>Example</span></div><p>{t.description}</p></article>; }

function Footer() { return <footer><div className="footer-main"><div><Link to="/" className="brand"><Logo/> <span>AnimeFusion</span></Link><p>A flexible core for your own anime platform.</p></div><div className="footer-cols"><div><small>PRODUCT</small><Link to="/features">Features</Link><Link to="/templates">Templates</Link></div><div><small>ACCOUNT</small><Link to="/signin">Sign in</Link><Link to="/signup">Get started</Link></div><div><small>DEVELOPERS</small><span>Documentation · future</span><span>GitHub · future</span></div></div></div><div className="footer-bottom"><span>© {new Date().getFullYear()} AnimeFusion</span><span>Built for self-hosting.</span></div></footer>; }

function Home() { return <><main>
  <section className="hero"><div className="hero-copy"><div className="eyebrow"><i/> ANIMEFUSION / PLATFORM</div><h1>The modern core<br/><span>for your anime site.</span></h1><p>A flexible foundation for building, managing and extending your own anime experience — with your infrastructure, your data and your visual identity.</p><div className="hero-buttons"><Button to="/signup">Get started</Button><Button to="/templates" secondary>Explore templates</Button></div><div className="hero-meta"><span>01 <b>SELF-HOSTED</b></span><span>02 <b>MODULAR</b></span><span>03 <b>EXTENSIBLE</b></span></div></div><div className="hero-visual"><div className="visual-label">PRODUCT PREVIEW <span>CONCEPT</span></div><ProductPreview/></div></section>
  <section className="marquee"><span>CORE LOGIC</span><i/> <span>CONTENT</span><i/> <span>PROVIDERS</span><i/> <span>TEMPLATES</span><i/> <span>ADMINISTRATION</span><i/> <span>EXTENSIONS</span></section>
  <section className="split-section intro"><div className="section-index">01 / FOUNDATION</div><div><h2>Built around<br/><span>the parts that matter.</span></h2><p>AnimeFusion separates core logic from presentation. The platform can handle the system while your template shapes the experience.</p><Link to="/features" className="under-link">Explore the architecture <span>↗</span></Link></div></section>
  <section className="feature-showcase"><div className="showcase-head"><div><small>02 / SYSTEM</small><h2>One core.<br/><span>Many directions.</span></h2></div><p>Keep the important pieces independent so the product can evolve without forcing every installation into the same visual language.</p></div><div className="editorial-features">{features.slice(0,3).map((f,i)=><article key={f[0]} className={i===1?'feature-wide':''}><small>{f[0]}</small><h3>{f[1]}</h3><p>{f[2]}</p><div className={`feature-art art-${i}`}><span/><i/><b/></div></article>)}</div></section>
  <section className="workflow-section"><div className="section-center"><small>03 / INTERACTIVE DEMO</small><h2>See the routing layer<br/><span>in motion.</span></h2><p>A conceptual workflow showing how a configurable provider layer can route a content request.</p></div><WorkflowGraph/></section>
  <section className="architecture-section"><div className="section-center"><small>04 / ARCHITECTURE</small><h2>Core logic stays separate<br/><span>from presentation.</span></h2></div><Architecture/></section>
  <section className="templates-section"><div className="showcase-head"><div><small>05 / TEMPLATE SYSTEM</small><h2>Start with a look.<br/><span>Make it yours.</span></h2></div><Link to="/templates" className="under-link">Browse all templates <span>↗</span></Link></div><div className="template-grid">{templates.slice(0,3).map(t=><TemplateCard key={t.slug} t={t}/>)}</div></section>
  <section className="cta"><small>06 / YOUR PLATFORM</small><h2>Build around<br/><em>a better core.</em></h2><p>Start with AnimeFusion and shape the experience around your product.</p><div><Button to="/signup">Get started</Button><Button to="/templates" secondary>Explore templates</Button></div></section>
</main><Footer/></>; }

function Features() { return <><main className="page"><section className="page-hero"><small>ANIMEFUSION / FEATURES</small><h1>The system behind<br/><span>your site.</span></h1><p>A reusable foundation for content, providers, administration, templates and developer extensions — designed to stay modular.</p></section><section className="feature-list">{features.map(f=><article key={f[0]}><small>{f[0]}</small><div><h2>{f[1]}</h2><p>{f[2]}</p></div><span>↗</span></article>)}</section><section className="demo-page"><div className="showcase-head"><div><small>LIVE DEMO</small><h2>Content routing,<br/><span>visualized.</span></h2></div><p>Hover or select a node to inspect the conceptual path. The demo is intentionally illustrative; it does not represent live production traffic.</p></div><WorkflowGraph/></section><section className="architecture-page"><div><small>ARCHITECTURE</small><h2>A replaceable presentation layer.</h2><p>The core owns the platform concerns. Templates consume that foundation and can change without requiring a new application architecture.</p></div><Architecture/></section></main><Footer/></>; }

function Templates() { return <><main className="page"><section className="page-hero"><small>ANIMEFUSION / TEMPLATES</small><h1>Start with a look.<br/><span>Make it yours.</span></h1><p>Conceptual frontend directions for AnimeFusion installations. Each one explores a different visual language while keeping the same core idea.</p></section><section className="market-intro"><span>EXAMPLE MARKETPLACE</span><p>These previews are design concepts, not hosted customer sites. Provider configuration and platform operation belong to your own installation.</p></section><div className="market-grid">{templates.map(t=><TemplateCard key={t.slug} t={t}/>)}</div></main><Footer/></>; }

function TemplateDetail({ slug }) { const t = templates.find(x=>x.slug===slug) || templates[0]; return <><main className="page detail"><section className="detail-head"><Link to="/templates" className="back">← Templates</Link><small>{t.label}</small><h1>{t.name}<span>.</span></h1><p>{t.description}</p><Button to="/signup">Use this direction</Button></section><div className="detail-preview"><TemplatePreview t={t}/></div><section className="detail-copy"><div><small>DESIGN PHILOSOPHY</small><h2>A visual layer<br/>without a new core.</h2></div><div><p>{t.name} is a conceptual template direction built to demonstrate how an AnimeFusion installation can carry its own personality.</p><p>The presentation layer is intentionally separated from providers, content and configuration so the underlying platform can remain reusable.</p></div></section></main><Footer/></>; }

function Auth({ signup=false }) { return <main className="auth"><div className="auth-panel"><Link to="/" className="brand"><Logo/><span>AnimeFusion</span></Link><small>{signup?'CREATE ACCOUNT':'SIGN IN'}</small><h1>{signup?'Create your account.':'Welcome back.'}</h1><p>{signup?'Account access for future marketplace purchases, downloads and licenses.':'Sign in to your AnimeFusion storefront account.'}</p><form onSubmit={e=>e.preventDefault()}>{signup&&<label>Name<input autoComplete="name" placeholder="Your name"/></label>}<label>Email<input type="email" autoComplete="email" placeholder="you@example.com"/></label><label>Password<input type="password" autoComplete={signup?'new-password':'current-password'} placeholder="••••••••"/></label>{signup&&<label>Confirm password<input type="password" autoComplete="new-password" placeholder="••••••••"/></label>}<button type="submit">{signup?'Create account':'Sign in'} <span>→</span></button></form><div className="auth-switch">{signup?'Already have an account?':'Need an account?'} <Link to={signup?'/signin':'/signup'}>{signup?'Sign in':'Create one'}</Link></div><small className="auth-disclaimer">Authentication is a frontend shell until a backend is connected.</small></div></main>; }

export default function App() { const [route,setRoute]=useState(getRoute()); useEffect(()=>{const h=()=>setRoute(getRoute()); window.addEventListener('popstate',h); return()=>window.removeEventListener('popstate',h)},[]); useEffect(()=>window.scrollTo(0,0),[route]); let content; if(route==='/') content=<Home/>; else if(route==='/features') content=<Features/>; else if(route==='/templates') content=<Templates/>; else if(route.startsWith('/templates/')) content=<TemplateDetail slug={route.split('/')[2]}/>; else if(route==='/signin') content=<Auth/>; else if(route==='/signup') content=<Auth signup/>; else content=<Home/>; return <><Nav/>{content}</>; }
